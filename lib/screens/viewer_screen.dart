import 'package:flutter/material.dart';
class ViewerItem extends StatelessWidget {
  const ViewerItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('hi'),
      ),
      body: Container(),
    );
  }
}
